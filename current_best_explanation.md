# current_best.py の方針とアイデア

## 全体の方針

`current_best.py` は、楽曲の情報から `popularity` を予測するための機械学習プログラムである。中心となる考え方は、単純な音響特徴量だけで予測するのではなく、曲名、アーティスト名、アルバム名、ジャンル、音響的な傾向などをできるだけ特徴量として取り出し、それを CatBoost という勾配ブースティング系の回帰モデルに学習させる、というものである。

大まかな流れは次の通りである。

1. `train.csv` と `test.csv` を読み込む。
2. 訓練データとテストデータに同じ曲が含まれていないか確認する。
3. 曲名やアーティスト名などの文字列情報から特徴量を作る。
4. 音響特徴量を組み合わせたり、ジャンルごとの平均値などを作ったりする。
5. CatBoostRegressor を 5-Fold 交差検証で学習する。
6. テストデータを予測し、予測値を 0 から 100 の範囲に収める。
7. 最後に、訓練データとテストデータで `track_id` が一致する曲については、訓練データの正解値で予測値を上書きする。

全体としては、「モデル自体を複雑に作り込む」というよりも、「予測に役立ちそうな情報を多方面から特徴量として追加し、CatBoost に任せる」という方針で書かれている。

## データの重複確認

最初に、訓練データとテストデータの間で同じ楽曲が存在するかを確認している。

確認方法は 2 種類ある。

- `track_id` が完全に一致する曲を探す。
- `track_name` と `artists` の組み合わせが一致する曲を探す。

これは、テストデータの中に訓練データと同じ曲が入っている場合、その曲の `popularity` は訓練データ側から分かってしまうためである。後半の「リークを活用した後処理」は、この確認結果を利用するための処理になっている。

ただし、これは一般的な予測モデルとしての性能を上げる方法ではなく、データ分割の性質を利用した処理である。課題やコンペのルールによっては、訓練データとテストデータの重複を使って正解値を上書きすることは不適切とみなされる可能性がある。

## 特徴量エンジニアリングの考え方

このプログラムの大きな特徴は、元の列をそのまま使うだけでなく、かなり多くの追加特徴量を作っている点である。

### 曲名からの特徴量

曲名 `track_name` に対して、TF-IDF と TruncatedSVD を使って数値特徴量を作っている。

TF-IDF は、曲名に含まれる単語の出現傾向を数値化する方法である。ただし、TF-IDF の結果は次元数が多くなりやすいため、TruncatedSVD によって 5 次元に圧縮している。

また、曲名に含まれる特定の語からフラグ特徴量も作っている。

- `Remix` を含むか
- `Live` を含むか
- `Acoustic` を含むか
- `feat.` を含むか

これらは、楽曲の種類やバージョンの違いが人気度に関係するかもしれない、という発想で作られている。

### アーティスト名からの特徴量

`artists` についても TF-IDF と TruncatedSVD を使い、10 次元の特徴量に変換している。複数アーティストがセミコロンで区切られている前提で、アーティスト名をトークンとして扱っている。

さらに、次のような特徴量も作っている。

- 参加アーティスト数 `num_artists`
- 先頭のアーティスト `main_artist`

アーティストの知名度や、コラボ曲かどうかが人気度に影響する可能性を考えた特徴量である。

### 出現回数とレアカテゴリの処理

アルバム名とメインアーティストについて、データ中の出現回数を数えている。

- `album_count`
- `main_artist_count`
- `is_single`

また、出現回数が少ないアルバム名やアーティスト名は、それぞれ `Rare_Album`、`Rare_Artist` にまとめている。

これは、出現回数が非常に少ないカテゴリをそのままモデルに入れると、特定のデータにだけ過度に合わせてしまう可能性があるためである。珍しいカテゴリをまとめることで、過学習を少し抑える狙いがある。

### 音響特徴量の組み合わせ

元からある音響特徴量を組み合わせて、新しい特徴量も作っている。

例として、次のようなものがある。

- `mode` と `valence` の掛け合わせ
- `mode` と `energy` の掛け合わせ
- `energy` と `loudness` の比
- `acousticness` と `loudness` の掛け合わせ
- 再生時間をミリ秒から秒に変換した `duration_sec`
- `danceability` と `energy` の掛け合わせ

これは、単独の特徴量だけでは表しにくい関係をモデルに渡すための工夫である。例えば、同じ `energy` でも長調か短調かによって人気度との関係が違うかもしれない、という考え方である。

## グループごとの統計量

`track_genre` と `main_artist` ごとに、音響特徴量の平均値と標準偏差を計算している。

対象になっている音響特徴量は次の通りである。

- `danceability`
- `energy`
- `loudness`
- `tempo`
- `acousticness`
- `duration_ms`

これにより、「その曲の特徴がジャンル内で普通なのか、それとも外れているのか」をモデルが判断しやすくなる。

さらに、ジャンル内での偏差値のような特徴量も作っている。

- `genre_loudness_hensachi`
- `genre_energy_hensachi`
- `genre_duration_hensachi`

例えば、単に `loudness` が高いか低いかだけでなく、「そのジャンルの中ではどれくらい大きい音なのか」を表す特徴量になっている。

## K-means によるクラスタ特徴量

音響特徴量を標準化したうえで、K-means により 10 個のクラスタに分けている。

これは、楽曲を音響的なタイプに分類するための特徴量である。例えば、明るくて踊りやすい曲、静かでアコースティック寄りの曲、テンポが速い曲といったまとまりを、明示的なルールではなくデータから作る狙いがある。

作られたクラスタ番号 `audio_cluster` は、カテゴリ特徴量として CatBoost に渡されている。

## CatBoost を使う理由

モデルには `CatBoostRegressor` が使われている。

CatBoost は、カテゴリ変数を扱いやすい勾配ブースティング系のモデルである。このデータには、アーティスト名、アルバム名、ジャンル、キー、拍子などのカテゴリ情報が多く含まれるため、CatBoost と相性がよい。

このプログラムでは、次の列をカテゴリ特徴量として指定している。

- `artists`
- `main_artist`
- `album_name`
- `explicit`
- `track_genre`
- `audio_cluster`
- `mode`
- `key`
- `time_signature`

数値特徴量とカテゴリ特徴量をまとめて扱い、非線形な関係を学習させることが狙いである。

## 5-Fold 交差検証

学習では 5-Fold 交差検証を使っている。訓練データを 5 分割し、4 つを学習用、1 つを検証用として使う処理を 5 回繰り返す。

この方法により、訓練データ全体に対して、モデルがどれくらい安定して予測できるかを確認できる。

また、各 Fold でテストデータの予測も行い、最後に平均している。これは、1 つのモデルだけで予測するよりも、複数のモデルの予測を平均することで結果を安定させるためである。

## ターゲットエンコーディング

交差検証のループ内で、`track_genre` ごとの平均 `popularity` を特徴量として追加している。これはターゲットエンコーディングと呼ばれる手法である。

重要なのは、この処理を Fold の中で行っている点である。検証データの正解値を使って平均値を作ると、評価結果が不自然に良くなってしまう。そこで、このプログラムでは学習用データだけからジャンルごとの平均値を計算し、それを検証データとテストデータに適用している。

この部分は、過学習や評価の水増しを避けるための比較的丁寧な実装になっている。

## 予測値の後処理

`popularity` は 0 から 100 の範囲の値なので、モデルの予測結果もその範囲に収めている。

具体的には、次の処理をしている。

- 1.0 未満の予測値を 0.0 にする。
- 予測値を 0 から 100 の範囲にクリップする。

これにより、明らかにあり得ない予測値を出力しないようにしている。

## リークを使った最終上書き

最後に、テストデータの `track_id` が訓練データにも存在する場合、その予測値を訓練データの `popularity` で上書きしている。

これはスコアを大きく上げる可能性がある処理である。なぜなら、同じ `track_id` の曲であれば、予測するまでもなく訓練データ側に正解値が存在しているからである。

ただし、この処理は注意が必要である。これはモデルが一般化して正しく予測できるようになったというより、訓練データとテストデータの重複を利用している。実運用や厳密な評価では使えない可能性が高い。

そのため、このプログラムには次の 2 つの性格が混ざっていると考えられる。

- CatBoost と特徴量エンジニアリングによって、通常の予測性能を上げる部分
- 訓練データとテストデータの重複を利用して、特定の評価データに対するスコアを上げる部分

レポートなどで説明する場合は、この 2 つを区別して書くのがよい。

## まとめ

`current_best.py` は、楽曲人気度の予測精度を高めるために、次のようなアイデアを組み合わせたプログラムである。

- 曲名やアーティスト名の文字列情報を TF-IDF と SVD で数値化する。
- `Remix` や `Live` など、曲名に含まれる意味のある語をフラグ化する。
- アーティストやアルバムの出現回数を使い、知名度やレア度に近い情報を入れる。
- 音響特徴量同士を掛け合わせ、単独の特徴量では表しにくい関係を作る。
- ジャンルやアーティストごとの平均・標準偏差を使い、相対的な特徴を表す。
- K-means により、音響的に似た曲のグループを特徴量にする。
- カテゴリ変数に強い CatBoost を使って学習する。
- 5-Fold 交差検証で予測を安定させる。
- 最後に、訓練データとテストデータで同じ `track_id` の曲がある場合は、正解値で上書きする。

全体として、特徴量を多く作って CatBoost に学習させる実践的なスコア改善型のコードである。一方で、最後のリーク利用は通常の機械学習モデルの性能とは別物なので、説明や提出時には扱いに注意が必要である。
